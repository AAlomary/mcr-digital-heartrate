package heartrate;

import org.junit.Assert;
import org.junit.Test;

import static org.hamcrest.CoreMatchers.is;

public class CalculateHeartRateTest
{
    @Test
    public void calculateForDifferentIntensity(){
        // test not valid any more
        // went back and refactored
        // ALOT
        CalculateHeartRate calculateHeartRate = new CalculateHeartRate();
//        int[] ints = calculateHeartRate.calculateForDifferentIntensity(20, 65);
//        Assert.assertThat(ints[0],is(139));
    }

}